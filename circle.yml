---
deployment:
  release:
    tag: /^[0-9]+(\.[0-9]+)*/
    commands:
      - go get -v github.com/tcnksm/ghr
      - make ghr VERSION=${CIRCLE_TAG} USER=${CIRCLE_PROJECT_USERNAME} REPO=${CIRCLE_PROJECT_REPONAME}
